# Return the number of workers in a list of BiocParallelParam objects

Return the number of workers in a list of BiocParallelParam objects

## Usage

``` r
bpnworkers.list(bplist)
```

## Arguments

- bplist:

  List of BiocParallelParam objects

## Value

A vector of the `bpnworkers` count in each list element
