# Run compartment inference

Run compartment inference

## Usage

``` r
getCompartments(
  obj,
  res,
  chr,
  group,
  targets,
  bootstrap,
  num.bootstraps,
  genome,
  assay,
  smooth = smooth,
  boot.parallel,
  bpparams
)
```
