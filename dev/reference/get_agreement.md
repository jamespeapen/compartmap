# Compute sign agreement of a matrix of singular values

Compute sign agreement of a matrix of singular values

## Usage

``` r
get_agreement(mat)
```

## Arguments

- mat:

  A matrix of compartment calls where each column is a single-cell or
  grouped set of singular values
