# Check the threshold at which an object's calls were filtered, returning 0 if not filtered

Check the threshold at which an object's calls were filtered, returning
0 if not filtered

## Usage

``` r
get_filter_threshold(x)
```

## Arguments

- x:

  A `CompartmentCall` object
