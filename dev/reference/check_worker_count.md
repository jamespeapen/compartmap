# Check that the number of requested workers is valid

Check that the number of requested workers is valid

## Usage

``` r
check_worker_count(
  bpparam,
  group,
  chr_count,
  bootstrap,
  avail_workers = parallelly::availableCores()
)
```
